import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerContainer: {
    backgroundColor: 'purple',
    padding: 20,
  },
  headerTxt: {
    fontWeight: 'bold',
    fontSize: 16,
    color: 'white',
  },
});
